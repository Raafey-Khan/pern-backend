# Node modules (packages installed via npm or yarn)
node_modules/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment variables (Sensitive info like API keys, passwords, etc.)
.env

# Optional OS-specific files (for macOS/Windows users)
.DS_Store      # macOS file that stores folder view options
Thumbs.db      # Windows thumbnail cache files

# Build files (if using TypeScript or Babel for transpiling)
dist/
build/

# Debugging files
.vscode/       # VSCode specific settings
*.swp          # Swap files created by text editors like vim

# Other dependencies
package-lock.json  # Optional: lock file generated by npm (recommended to commit though)
yarn.lock          # Optional: lock file generated by yarn (recommended to commit though)